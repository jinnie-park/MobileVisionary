# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MobileVisionary' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MobileVisionary

  pod 'SwiftyJSON', '~> 3.1'

  target 'MobileVisionaryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MobileVisionaryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
